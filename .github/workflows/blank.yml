name : ci 
on: [push]

jobs:
  build :
    runs-on: self-hosted 
    steps :
    - uses: actions/checkout@v2
      
    - name: run echo comande
      run:  apt update -y
    - name: install apache 
      run:  apt install httpd -y 
